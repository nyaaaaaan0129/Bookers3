<div class="container">
  <div class="row">
  	<%= render 'shared/sidebar' %>
  	<div class="col-xs-9">
  	  <h2>Books</h2>
      
  		<table class="table">

            <thead>
              <th></th>
              <th>Title</th>
              <th>Opinion</th>
            </thead> 

            <tbody>
            <% @books.each do |book| %>
              <tr>
                <td><%= attachment_image_tag book.user, :profile_image, format: 'jpeg', class: "img-square", fallback: "no_image.jpg", size: "60x60" %></td>
                <td><%= book.title %></td>
                <td><%= book.body %></td>
              </tr>
            <% end %>
            </tbody>
        </table>
  	</div>
  </div>
</div>